name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - target: linux-x64
            runner: ubuntu-latest
          - target: windows-x64
            runner: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: cd packages/cli && bun install --frozen-lockfile
      - run: bun scripts/patch-opentui-reconciler.js
      - name: Build binary (Linux)
        if: matrix.target != 'windows-x64'
        env:
          FILIKS_VERSION: ${{ github.ref_name }}
          FILIKS_CLERK_FRONTEND_API: ${{ secrets.CLERK_FRONTEND_API }}
          FILIKS_CLERK_OAUTH_CLIENT_ID: ${{ secrets.CLERK_OAUTH_CLIENT_ID }}
        run: cd packages/cli && bun run scripts/build-binary.ts
      - name: Build binary (Windows)
        if: matrix.target == 'windows-x64'
        env:
          FILIKS_VERSION: ${{ github.ref_name }}
          FILIKS_CLERK_FRONTEND_API: ${{ secrets.CLERK_FRONTEND_API }}
          FILIKS_CLERK_OAUTH_CLIENT_ID: ${{ secrets.CLERK_OAUTH_CLIENT_ID }}
        shell: pwsh
        run: |
          curl.exe -fsSL https://github.com/oven-sh/bun/releases/download/bun-v1.3.14/bun-windows-x64-baseline.zip -o bun-baseline.zip
          tar -xf bun-baseline.zip
          $bun = Get-ChildItem -Recurse -Filter bun.exe | Select-Object -First 1 -ExpandProperty FullName
          cd packages/cli
          $env:BUN_BINARY = $bun
          bun run scripts/build-binary.ts
      - run: cd packages/cli/dist && tar -czf filiks-${{ matrix.target }}.tar.gz --exclude='*.tar.gz' filiks*
      - uses: actions/upload-artifact@v4
        with:
          name: filiks-${{ matrix.target }}
          path: packages/cli/dist/filiks-${{ matrix.target }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: filiks-*
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: filiks-*.tar.gz
          generate_release_notes: true
